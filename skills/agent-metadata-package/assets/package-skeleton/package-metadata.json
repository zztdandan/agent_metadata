{
  "$schema": "schema/package-metadata.schema.json",
  "kind": "agent-metadata-package",
  "schemaVersion": "0.2",
  "package": {
    "id": "example-agent-metadata",
    "version": "0.2.2",
    "name": "Example Agent Metadata",
    "description": "替换为包说明"
  },
  "capabilities": [
    {
      "id": "example-agent",
      "path": "capabilities/example-agent"
    }
  ],
  "bootstrap": "BOOTSTRAP.md",
  "distribution": {
    "exclude": [
      "dist/**",
      "**/.env",
      "**/sessions/**",
      "**/logs/**",
      "**/state.db*"
    ]
  }
}
