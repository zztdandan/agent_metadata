# 自举规约

自举智能体必须：

1. 读取 `README.md`、`package-metadata.json` 与本文件，识别能力域、资产和安全边界。
2. 确认当前宿主、版本、目标工作区、用户授权范围和缺失配置。
3. 将 canonical 资产映射到宿主支持的表达；不得把包外 Secret 写回包内。
4. 对技能保持完整目录，对工作区资产复制目录内容而不是额外嵌套资产目录。
5. 以静态验证、生成物验证、安装状态验证和宿主原生发现验证证明结果。

不得只因文件已经复制就报告成功；不得覆盖用户已有配置或可变工作区资产，除非用户已明确授权。
